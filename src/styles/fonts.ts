import { css } from 'styled-components';

const fonts = css`
  @font-face {
    font-family: 'Rock';
    font-style: normal;
    font-weight: 400;
    font-display: swap;
    src: url(/fonts/rock.woff2) format('woff2');
  }
`;

export default fonts;
