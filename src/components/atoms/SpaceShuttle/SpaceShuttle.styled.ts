import { motion } from "framer-motion";
import styled from "styled-components";

export const SpaceShuttle = styled(motion.div)`
  position: absolute;
  left: 50%;
  top: 100%;
  transform: translateX(-50%);
`;